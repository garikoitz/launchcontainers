for the nifti prepare,

first we run prepare_func, which will call local_nordic.sh

In the meantime we can run prepare_anat, which we will call local_run_presurfer

When both of them finished, approx 1h for each session, we run prepare_fmap

Second we will run python 01_prepare_fmap_folder.py, it will copy and create the sub-/ses-/fmap folder
and copy the scans.tsv file

then we run 02_auto_gen_intendedfor.py to populate the intended for fields in the fmap for fmriprep


For this step, we only need to input subseslist name, i.e. subseslist_sub08.txt

the file will be put under base/code


## dont' change subseslist in the middle of running the prepare_anat and prepare_func, 
## it will influence the running program

prepare_fmap will be last, it needs to wait for:
1. fix_func_sbref finished
2. merge_nifti_session finished
3. NORDIC func finished

this 1-2-3 will be the current workflow for now, 
still I am not so sure, if the fmap populate intended for is working
and based on the scans.tsv that with the _magnitude, instead of _bold
